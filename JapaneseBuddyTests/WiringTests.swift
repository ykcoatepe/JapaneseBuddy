import XCTest
@testable import JapaneseBuddyProj   // if your app target name differs, change this

final class WiringTests: XCTestCase {
    func testTargetWired() {
        XCTAssertTrue(true) // just proves the test target runs
    }
}

