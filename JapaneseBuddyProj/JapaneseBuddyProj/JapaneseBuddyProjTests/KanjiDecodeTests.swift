#if !canImport(Testing)
import Foundation

// Placeholder to satisfy legacy project path without pulling test symbols
// into targets that already compile Kanji decode tests from the main tests dir.
private enum _KanjiDecodePlaceholder {}
#endif
